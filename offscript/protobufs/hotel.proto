syntax = "proto3";
package protoproto;

// from insie this folder run 
// protoc --go_out=. hotel.proto

option go_package = "./protobuf";

message hotel {
  int32 id = 1;
  string name = 2;
  string address = 3;
  float lattitude = 4;
  float longitude = 5;
}